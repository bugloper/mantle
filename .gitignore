/bin/
*.pem

# Stray binaries from `go build ./cmd/...`, which writes to the current
# directory rather than ./bin.
/mantled
/mantle
/mantle-ui

# Release artifacts built by contrib/release.sh
/dist/
